[package]
name = "qubit-redact-serde-missing-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-redact = {
    version = "0.4",
    features = ["serde"],
}
qubit-redact-derive = { path = "../../../.." }
