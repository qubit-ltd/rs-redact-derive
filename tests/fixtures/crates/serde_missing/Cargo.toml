[package]
name = "qubit-redact-serde-missing-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-redact = {
    version = "0.5",
    path = "../../../../../rs-redact",
    features = ["serde"],
}
qubit-redact-derive = { path = "../../../.." }
