[package]
name = "serde"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
serde_impl = { package = "serde", version = "1" }
qubit-redact = {
    version = "0.5",
    features = ["serde"],
}
qubit-redact-derive = { path = "../../../.." }
