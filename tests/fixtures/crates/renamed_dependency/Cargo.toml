[package]
name = "redact-renamed-dependency-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
qubit-redact-derive = { path = "../../../.." }
redaction-runtime = {
    package = "qubit-redact",
    version = "0.5",
    default-features = false,
}
